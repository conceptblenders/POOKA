# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp

# draw.io backups and locks
*.dtmp
*.bkp
~$*

# iCloud / cloud-sync conflict copies (e.g. "file 2.md")
* [0-9].md
* [0-9].drawio
*.icloud

# --- Website build ---------------------------------------------------------
# Python environment
.venv/
venv/
__pycache__/
*.py[cod]

# MkDocs output and caches
site/
.cache/

# Generated by scripts/sync_docs.py from the canonical sources. Never edit these
# by hand and never commit them: paper/, references/ and figures/ are the truth.
docs/paper/
docs/reference/
docs/assets/images/
build/

# The PDF edition, rendered from the canonical chapters by scripts/build_pdf.py.
# It is a build artifact: CI rebuilds it on every deploy, so it is not committed.
docs/assets/pdf/

# The brand font is licensed and deliberately not distributed. The logo ships as
# outlines in docs/assets/brand/ instead; see scripts/make_wordmark.py.
*.ttf
*.otf
